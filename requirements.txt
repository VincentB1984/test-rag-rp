# RAG Recensement v4 — requirements.txt
# IMPORTANT : torch, sentence-transformers et transformers sont SUPPRIMÉS.
# Le reranking se fait via l'API Albert (HTTP), sans modèle local.
# Cela évite le téléchargement de 2Go de dépendances CUDA sur Render.

# Serveur web
fastapi
uvicorn[standard]
python-multipart

# LangChain — versions sans contraintes pour éviter les conflits de dépendances
langchain
langchain-community
langchain-openai
langchain-core

# Base vectorielle
faiss-cpu

# Chargement de documents
pypdf
lxml
odfpy==1.4.1
openpyxl
xlrd==2.0.1

# Recherche web
duckduckgo-search

# HTTP
requests

# Utilitaires
python-dotenv
